{
  "name": "content_filter",
  "version": "1.0.0",
  "description": "Filters appendixes on visibility",
  "main": "./library.js",
  "scripts": {
    "build": "webpack --entry ./library.js --output-path . --output-filename library.min.js --progress --mode=production --devtool source-map",
    "watch": "webpack --entry ./library.js --output-path . --output-filename library.min.js --progress--devtool source-map",
    "all": "run-p build"
  },
  "author": "Ewald Harmsen",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.107.2",
    "webpack-cli": "^4.10.0"
  }
}
